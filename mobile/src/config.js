// Central place for the backend URL so it isn't duplicated/hardcoded across files.
// Replace with your machine's LAN IP (or a proper API domain) when testing on a device.
export const API_URL = 'http://localhost:3333';
